import { PagePlaceholder } from '../_placeholder';

export default function ReferenceHomePage() {
  return (
    <PagePlaceholder
      title="快查"
      step="Step 8"
      pendingItems={[
        '分類網格：生命徵象 / 藥物 / 疾病 / 衛教',
        '列表與卡片詳情頁',
        '依 type 自動掛 Safety Banner（SB-1 / SB-3 / SB-8）',
      ]}
    />
  );
}
